// src/core/config/GameConfig.ts
export const GameConfig = {
    CARDS: {
        COUNT: 144,
        INTERVAL_MS: 1000,
        DURATION_SEC: 2,
        STACK_OFFSET_Y: -0.5,
        SCALE: 0.5,
    },
    FIRE: {
        PARTICLE_COUNT: 10,
        // We'll add more here tomorrow!
    },
    SLOT: {
        // For Task 3
    }
} as const;